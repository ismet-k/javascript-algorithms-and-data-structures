/**
 * Defines an object that maps currency unit names to their corresponding values.
 * The values represent the monetary worth of each currency unit.
 */
const currencyUnits = {
  PENNY: 0.01,
  NICKEL: 0.05,
  DIME: 0.1,
  QUARTER: 0.25,
  ONE: 1,
  FIVE: 5,
  TEN: 10,
  TWENTY: 20,
  "ONE HUNDRED": 100,
};

export default currencyUnits;
